export function Footer() {
  return (
    <footer className="site-footer">
      Demo storefront for scraper-resilience testing — layout rotates every few loads,
      product data stays valid underneath. Not a real store.
    </footer>
  );
}
